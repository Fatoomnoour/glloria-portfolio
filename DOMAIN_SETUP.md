# ربط دومين Glloria عبر Vercel

هذا الدليل يشرح إعداد نطاق مثل `glloria.studio` أو `glloria.com` بعد نشر المشروع من مستودع GitHub إلى Vercel.

## 1. إنشاء مشروع Vercel

1. افتحي Vercel واختاري **Add New → Project**.
2. اربطي مستودع GitHub الخاص بـ Glloria: `Fatoomnoour/glloria-portfolio`.
3. اختاري فرع `main`، واتركي أمر البناء `pnpm build` ومجلد الإخراج `dist/public` كما هما.
4. أضيفي قيم البيئة من ملف `.env.example` داخل **Project Settings → Environment Variables**. لا تضعي أي أسرار داخل GitHub.

> قاعدة البيانات، OAuth، وStorage يجب أن تكون متاحة من بيئة الإنتاج قبل اختبار الحجز أو `/admin`. لا تستخدمي بيانات وهمية كبديل.

## 2. إضافة الدومين

من **Vercel → Project → Settings → Domains** أضيفي النطاق الأساسي، مثل `glloria.studio`، ثم أضيفي `www.glloria.studio` إذا رغبتِ في دعمه. اختاري نسخة أساسية واحدة فقط لتكون الـ canonical.

## 3. سجلات DNS

استخدمي القيم التي يعرضها Vercel للمشروع؛ قد تختلف حسب مزود النطاقات. في الإعداد المعتاد:

- النطاق الأساسي: سجل `A` للرمز `@` إلى `76.76.21.21`.
- نطاق `www`: سجل `CNAME` إلى `cname.vercel-dns.com`.

احذفي سجلات التوجيه المتعارضة فقط بعد التأكد من أنها تخص الاستضافة القديمة. لا تضيفي أكثر من سجل متعارض لنفس الاسم.

## 4. www و HTTPS

اختاري نسخة واحدة أساسية، ثم اضبطي إعادة التوجيه من النسخة الأخرى داخل Vercel. بعد انتشار DNS، يصدر Vercel شهادة SSL تلقائيًا. اختبري فتح النطاق عبر `https://` وتحققي من عدم ظهور تحذير أمني.

## 5. إعدادات الإنتاج

بعد ربط الدومين، حدّثي في التطبيق:

- canonical URL.
- Open Graph وTwitter metadata.
- sitemap وrobots.
- روابط المشاركة الاجتماعية.
- رابط callback الخاص بـ OAuth إن كان مزود المصادقة يتطلب تسجيله مسبقًا.

## 6. فحص ما بعد النشر

اختبري الصفحة الرئيسية، `/projects`، صفحات تفاصيل المشاريع، `/booking`، `/contact`، `/privacy`، `/terms`، و`/admin`. تحققي من اللغة العربية RTL والإنجليزية LTR، معرض الصور، واتساب، الحجز، وعدم وجود تمرير أفقي على الهاتف.

أنشئي حجزًا تجريبيًا من بيئة غير الإنتاج فقط، ثم تأكدي من ظهوره في لوحة الإدارة وتغيّر حالته دون كشف أي بيانات للعملاء في الصفحات العامة.

## 7. بيانات العملاء

تظل بيانات الحجوزات داخل لوحة الإدارة المحمية. لا تشاركي رابط `/admin` مع العملاء، ولا تضعي بيانات العملاء داخل ملفات عامة أو لقطات شاشة. تكامل البريد وGoogle Calendar وWhatsApp API يحتاج إعدادًا خارجيًا منفصلًا إذا رغبتِ في تفعيله لاحقًا.
