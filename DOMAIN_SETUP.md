# ربط دومين Glloria المخصص

هذا الدليل يشرح إعداد نطاق مثل `glloria.studio` أو `glloria.com` بعد شراء الدومين من مزود النطاقات.

## 1. إضافة الدومين

من لوحة إدارة مشروع Glloria افتحي **Settings → Domains**، ثم اختاري إضافة دومين مخصص واكتبي النطاق الأساسي الذي تريدينه. يفضّل اختيار نسخة أساسية واحدة، مثل `glloria.studio`، واستخدام `www.glloria.studio` كنسخة بديلة تعيد التوجيه إلى النطاق الأساسي.

## 2. سجلات DNS

سيعرض لكِ مدير النطاقات القيم الدقيقة الخاصة بالمشروع. أضيفي السجلات كما تظهر في لوحة الإدارة، ولا تستخدمي قيماً قديمة من مزود آخر. عادةً ستحتاجين إلى سجل `A` أو `CNAME` للنطاق الأساسي، وسجل `CNAME` لـ `www` إذا طلبته لوحة الاستضافة. احذفي سجلات التوجيه المتعارضة فقط بعد التأكد من أنها تخص الموقع القديم.

## 3. SSL وHTTPS

بعد انتشار DNS، تتحقق الاستضافة من النطاق وتصدر شهادة SSL تلقائياً عند دعمها. اختبري فتح `https://` وليس `http://`، وتحققي من أن المتصفح لا يعرض تحذيراً أمنياً. لا ترفعي شهادات يدوية إلى المشروع إلا إذا طلب مزود الاستضافة ذلك صراحةً.

## 4. النطاق الأساسي وwww

اختاري نسخة واحدة كـ canonical. إذا كان الأساسي هو `glloria.studio`، يجب أن تعيد `www.glloria.studio` التوجيه إليه، والعكس صحيح إن اخترتِ www. بعد الربط حدّثي `canonical` وOpen Graph و`robots.txt` و`sitemap.xml` بالقيمة النهائية.

## 5. النسخة القديمة

لا يمكن ضمان إعادة توجيه نطاق `manus.space` من داخل كود الموقع إذا لم تملكِ التحكم في إعداداته. اتركيه يعمل مؤقتاً، ثم استخدمي إعدادات النطاقات في لوحة الإدارة أو مزود الاستضافة لإنشاء redirect دائم 301 إن كانت الخاصية متاحة.

## 6. بعد الربط

افتحي الصفحة الرئيسية، `/projects`، `/booking`، `/contact`، `/privacy`، و`/terms` على الدومين الجديد. تحققي من روابط WhatsApp وInstagram وFacebook، ومن ظهور عنوان `Glloria | تصميم داخلي في قنا – المهندسة هبة الدمراني` عند مشاركة الرابط. يجب تحديث رابط sitemap في Search Console وإرسال النسخة الجديدة للفهرسة.

## 7. بيانات العملاء

تظل بيانات الحجوزات داخل لوحة الإدارة المحمية. لا تضيفي بيانات اختبار إلى الموقع العام، ولا تشاركي رابط `/admin` مع العملاء. تكامل البريد وGoogle Calendar وWhatsApp API يحتاج إعداداً خارجياً منفصلاً إذا رغبتِ في تفعيله لاحقاً.
