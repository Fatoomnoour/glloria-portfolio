# مراجعة فنية شاملة — موقع Glloria

**التاريخ:** 5 سبتمبر 2026 · **الفرع:** `arena/01a071c9-glloria-portfolio` · **الكوميت:** `9cf0997`

> ### ✅ حالة التنفيذ — المرحلة صفر والأولى (تقنياً) مكتملتان
> **المرحلة صفر:** النقاط 1، 2، 3، 12، 24، 38
> **المرحلة الأولى:** النقاط 4، 13، 14، 15، 19، 20، 21، 22، 23، 25، 26، 27
> **المرحلة الثانية (الجزء التقني):** النقاط 5، 6، 7، 10، 11، 12، 28، 36، 37
> إجمالي **107 اختبار ناجح** (كان 36). التفاصيل في نهاية الملف.
> **المرحلة الثالثة (التقنية):** النقاط 9، 11، 16، 17، 41، 43، 44، 45
> **ما زال مفتوحاً:** الصور الحقيقية وبيانات المشاريع (29–35) — **محجوبة: الملفات لا تصل للمستودع**؛ والنطاق المخصص (8)، وبديل الإشعار البريدي (39)، واتصالات MySQL على Serverless (40)، و ESLint (42).

## الملخّص التنفيذي

الكود مبني بشكل جيد من ناحية الهندسة (TypeScript يمرّ بلا أخطاء، 36 اختبار ناجح، البناء يعمل، الهوية البصرية متماسكة، نموذج الحجز متعدد الخطوات ممتاز التصميم). **لكن الموقع في وضعه الحالي لا يستطيع تحويل زائر إلى عميل** — لأن قناتَي التواصل الرئيسيتين (الحجز والاتصال) مكسورتان فعلياً في الإنتاج، ولأن SEO المحلي شبه معدوم.

| المحور | التقييم |
|---|---|
| جودة الكود والبنية | 8 / 10 |
| **تحويل العملاء (Conversion)** | **2 / 10** ← أخطاء حرجة |
| SEO ومحركات البحث | 3 / 10 |
| الأداء | 5 / 10 |
| الوصولية (a11y) | 6 / 10 |
| المحتوى والمصداقية التجارية | 3 / 10 |
| الأمان والتشغيل | 6 / 10 |

---

# 🔴 القسم الأول: أخطاء حرجة — تُفقدك عملاء الآن

## 1. نموذج الحجز يُظهر "فشل الإرسال" رغم أنه حفظ الطلب فعلاً ⚠️ الأخطر

`server/routers.ts:187` ينادي `notifyOwner()` **بعد** حفظ الطلب في قاعدة البيانات. وفي `server/_core/notification.ts:71` الدالة **ترمي استثناء** إذا لم تكن متغيرات `BUILT_IN_FORGE_API_URL` / `BUILT_IN_FORGE_API_KEY` مضبوطة — وهي غير مضبوطة على Vercel (موثّق في `client/src/data/projectImages.ts` أن Forge غير متاح على هذا النشر، و`VERCEL_DEPLOYMENT.md` يصنّفها كاختيارية).

أثبتُّ ذلك عملياً:

```
$ npx tsx poc.mts   # مع إفراغ متغيرات Forge
THREW: INTERNAL_SERVER_ERROR - Notification service URL is not configured.
```

**سلسلة الضرر:**
1. العميل يملأ 5 خطوات كاملة ويضغط "إرسال طلب الموعد"
2. الطلب **يُحفظ في قاعدة البيانات بنجاح**
3. ثم يُرمى خطأ 500
4. العميل يرى: *"تعذر إرسال الطلب حالياً. حاولي مرة أخرى"* → يعيد الإرسال → **صفوف مكرّرة**
5. هبة **لا تصلها أي إشعارات** بأي حال
6. العميل يغادر مقتنعاً أن الموقع لا يعمل

**الإصلاح (سطرين):**
```ts
// server/routers.ts — لا تدع الإشعار يُسقط الطلب أبداً
const notified = await notifyOwner({ ... }).catch(err => {
  console.error("[Booking] owner notification failed", err);
  return false;
});
```
وبشكل أعمق: `notifyOwner` يجب أن تُرجع `false` بدل الرمي عند غياب الإعداد (تعليقها الخاص يقول ذلك حرفياً: *"Returns false when the upstream service cannot be reached"* — لكن التنفيذ يخالف التوثيق).

## 2. نموذج "تواصل معنا" لا يُرسل أي شيء إلى أي مكان

`client/src/pages/Contact.tsx:19`

```tsx
const handleSubmit = (event: FormEvent<HTMLFormElement>) => {
  event.preventDefault();
  setSent(true);           // ← هذا كل ما يحدث
};
```

النموذج يعرض للزائر: **"تم استلام رسالتك — وصلت التفاصيل إلى Glloria"** بينما البيانات تُمحى فوراً من الذاكرة. كل استفسار من هذه الصفحة **ضائع 100%**. هذا ليس خطأ تقنياً فقط — بل مضلّل للمستخدم (ووعد كاذب صريح في نص الواجهة).

**الإصلاح:** إما ربطه بـ `trpc.consultations.create` (بنفس نمط صفحة الحجز)، أو — الأسرع — تحويله إلى نموذج يفتح WhatsApp برسالة معبّأة، أو حذفه نهائياً وتوجيه الزائر إلى `/booking`.

## 3. سكربت التحليلات يُنشر مكسوراً في الإنتاج

`client/index.html:100` يستخدم `%VITE_ANALYTICS_ENDPOINT%` — ومخرجات البناء تؤكد:

```
(!) %VITE_ANALYTICS_ENDPOINT% is not defined in env variables found in /index.html
(!) %VITE_ANALYTICS_WEBSITE_ID% is not defined
<script src="%VITE_ANALYTICS_ENDPOINT%/umami"> can't be bundled without type="module"
```

وفي `dist/public/index.html` السطر 102 يبقى حرفياً `src="%VITE_ANALYTICS_ENDPOINT%/umami"` → المتصفح يطلب مساراً وهمياً (404) على **كل زيارة**، ولا يُسجَّل أي تحليل. النتيجة: أنت تدير موقعاً تجارياً **بدون أي بيانات زوار إطلاقاً**.

**الإصلاح:** اضبط المتغيرين في Vercel، أو (الأفضل) استبدل Umami بـ Vercel Analytics / Plausible وأزل الوسم.

## 4. القائمة الرئيسية تُعيد تحميل الصفحة بالكامل في كل نقرة

`client/src/App.tsx:74` — كل عناصر القائمة `<a href>` عادية وليست `<Link>` من wouter:

```tsx
{navItems.map(item => (
  <a key={item.href} href={item.href} ...>   // ← إعادة تحميل كامل
```

الموقع SPA لكن التنقل الأساسي يعمل كموقع 1998: تحميل كامل للـ JS و CSS و الصور في كل نقرة. يقتل سرعة التصفح وينسف فائدة code-splitting الموجود أصلاً. كما أن `location === item.href` لا يطابق `/#about` أبداً → حالة "الصفحة النشطة" لا تعمل لثلاثة عناصر.

**الإصلاح:** استخدم `<Link>` للمسارات، وعالج روابط `#` بمعالج تمرير (scroll) يعمل عبر الصفحات.

## 5. صور المشاركة الاجتماعية (OG) بمسار نسبي — المشاركة تظهر بلا صورة

`client/index.html:34`
```html
<meta property="og:image" content="/images/hero-living-room-1920w.webp" />
```
فيسبوك وواتساب وLinkedIn **يتطلبون رابطاً مطلقاً**. عند مشاركة الموقع على واتساب (قناة العملاء الأساسية عندك!) لن تظهر أي صورة. نفس المشكلة في `image` داخل JSON-LD.

**الإصلاح:** `https://النطاق/images/glloria-og-1200x630.jpg` — ويُفضّل صورة OG مخصّصة بمقاس 1200×630 بصيغة JPG (WebP غير مدعوم في بعض معاينات واتساب).

---

# 🟠 القسم الثاني: SEO — الموقع شبه غير مرئي لجوجل

## 6. لا توجد عناوين ولا أوصاف خاصة بكل صفحة

الموقع SPA خالص بلا SSR ولا prerender ولا `react-helmet`. النتيجة:
- `/projects`، `/booking`، `/contact`، `/projects/boska` — **كلها تحمل نفس الـ `<title>` ونفس الـ description**
- جوجل يرى HTML شبه فارغ (`<div id="root"></div>`) عند أول زحف
- `<link rel="canonical" href="https://glloria-portfolio.vercel.app/">` **ثابت على الرئيسية في كل المسارات** → جوجل قد يستبعد كل الصفحات الداخلية من الفهرسة كنسخ مكرّرة

**الإصلاح المقترح:** إضافة `vite-plugin-prerender` أو `vite-react-ssg` لتوليد HTML ثابت لكل مسار (المحتوى شبه ثابت أصلاً)، أو الترحيل إلى Next.js إن كان هناك نية للتوسع.

## 7. لا يوجد hreflang ولا مسارات لغوية

الموقع ثنائي اللغة لكن اللغة تُبدَّل بـ `localStorage` فقط على نفس الـ URL. النسخة الإنجليزية **غير موجودة لدى جوجل إطلاقاً**. الحل الصحيح: `/ar/...` و `/en/...` مع `<link rel="alternate" hreflang="ar-EG">` و `hreflang="en"` و `hreflang="x-default"`.

## 8. قسم FAQ كامل بلا FAQPage schema

عندك 6 أسئلة شائعة ممتازة في `Home.tsx:96` — بإضافة JSON-LD من نوع `FAQPage` تحصل على **نتائج غنية (rich snippets)** في جوجل تضاعف نسبة النقر. فرصة مجانية ضائعة.

## 9. النطاق المؤقت مثبت في كل مكان

`glloria-portfolio.vercel.app` مكتوب حرفياً في: `index.html` (canonical, og:url, JSON-LD ×3)، `robots.txt`، `sitemap.xml`. **لا يمكن إطلاق مشروع مقاولات احترافي على نطاق vercel.app** — ابنِ الروابط من متغير بيئة `VITE_SITE_URL` وسجّل نطاقاً مثل `glloria.studio` (البريد يستخدمه أصلاً!).

## 10. بيانات LocalBusiness ناقصة وغير قياسية

```json
"telephone": "+20 10 666 46397"   ← يجب: "+201066646397" (E.164)
```
ناقص أيضاً: `geo` (إحداثيات)، `openingHours`، `logo`، `founder` (Person schema لهبة)، `aggregateRating`، `hasOfferCatalog` للخدمات، `Service` schema لكل خدمة. كل هذه عناصر تُظهرك في **الحزمة المحلية (Local Pack)** لبحث "مهندسة ديكور قنا".

## 11. sitemap يدوي وناقص

`sitemap.xml` ملف ثابت: بلا `<lastmod>`، بلا `<priority>`، ولا يشمل المشاريع التي تضيفها هبة من لوحة الإدارة (يذكر `interior` و `boska` فقط). أي مشروع جديد **لن يُفهرس أبداً**. يجب توليده ديناميكياً من قاعدة البيانات.

## 12. `robots.txt` لا يمنع `/admin`

سطران فقط `Allow: /`. أضف `Disallow: /admin`.

---

# 🟠 القسم الثالث: الأداء

## 13. صورة الـ Hero: 391 كيلوبايت تُحمَّل إجبارياً على الموبايل

```html
<link rel="preload" as="image" href="/images/hero-living-room-1920w.webp" fetchpriority="high" />
```
Preload **بلا** `imagesrcset` / `imagesizes` → كل زائر موبايل (وهم الأغلبية الساحقة في مصر) ينزّل نسخة 1920px بحجم **391 KB** بدل نسخة 480px بحجم 30 KB. هذا وحده يدمّر مقياس LCP في Core Web Vitals — وهو عامل ترتيب مباشر في جوجل.

**الإصلاح:**
```html
<link rel="preload" as="image"
      imagesrcset="/images/hero-living-room-480w.webp 480w, ... 1920w"
      imagesizes="100vw" fetchpriority="high" />
```
وأضف نسخ AVIF (توفّر ~30% إضافية).

## 14. `display=optional` قد يمنع ظهور الخطوط نهائياً

`index.html:57` — مع `display=optional` إذا تأخّر الخط عن ~100ms **لا يُستخدم في هذه الزيارة إطلاقاً**، والنص يظهر بخط النظام الاحتياطي. على شبكات 3G المصرية هذا يعني أن هوية الموقع البصرية (الجزء الأهم في موقع تصميم داخلي!) **تختفي**.

أيضاً: 4 عائلات خطوط (Alexandria + IBM Plex Sans Arabic + Cormorant Garamond + DM Mono) بـ 13 وزناً = عبء ثقيل. اختصرها إلى عائلتين، استضفها ذاتياً (`@fontsource`) بدل Google Fonts، واستخدم `display=swap`.

## 15. لا توجد رؤوس تخزين مؤقت (Cache-Control)

`vercel.json` يضبط رؤوس الأمان فقط. الأصول ذات البصمة (`/assets/index-B4rRzctO.css`) **يجب** أن تحمل `Cache-Control: public, max-age=31536000, immutable`، والصور في `/images/*` كذلك. حالياً كل زائر عائد يُعيد التحقق من كل شيء.

## 16. ملف CSS واحد بحجم 194 KB على كل صفحة

`client/src/index.css` = **5,785 سطر** في ملف واحد يشمل تنسيقات لوحة الإدارة (`admin-editor`، `dashboard-*`، `analytics-*`) — تُحمَّل على الصفحة الرئيسية للزوار العاديين. افصل CSS الأدمن في ملف يُستورد داخل `Admin.tsx` فقط.

## 17. حزمة الأدمن 535 KB (تحذير البناء)

```
Admin-BNKxV1gW.js  535.18 kB │ gzip: 144.98 kB
(!) Some chunks are larger than 500 kB
```
Recharts هو الجاني غالباً — حمّل `AdminAnalytics` بـ `lazy()` منفصل.

## 18. ملف تشخيص تطويري يُنشر للإنتاج

`client/public/__manus__/debug-collector.js` — 25 KB يعترض كل الـ console و fetch و XHR وأحداث المستخدم ويرسلها إلى `/__manus__/logs`. غير مُشار إليه في `index.html` لكنه **متاح للعامة** (تحققت: `GET /__manus__/debug-collector.js → 200`). احذفه من `client/public/`.

---

# 🟠 القسم الرابع: الوصولية وجودة الواجهة

## 19. تباين ألوان دون معايير WCAG AA

حسبتُ نسب التباين على الخلفية `#f5f1ea`:

| اللون | الاستخدام | النسبة | الحكم |
|---|---|---|---|
| `#a39d93` | نص placeholder في النماذج | **2.39 : 1** | ❌ فشل (المطلوب 4.5) |
| `#d38b70` | `--clay-light` | **2.42 : 1** | ❌ فشل |
| `#7b8064` | `--olive` | **3.65 : 1** | ❌ فشل |
| `#b65e45` | `--ring` (حلقة التركيز!) | **3.99 : 1** | ❌ فشل (المطلوب 3.0 للعناصر غير النصية — يمر بالكاد لكنه ضعيف) |
| `#9e503a` | `--clay` نص | 5.09 : 1 | ✅ |
| `#5f5952` | نص ثانوي | 6.14 : 1 | ✅ |

نصوص الـ placeholder غير مقروءة عملياً لكبار السن أو تحت ضوء الشمس — وهي داخل نماذج الحجز بالذات.

## 20. `<main>` داخل `<main>` — HTML غير صالح

`App.tsx:230` يلفّ المحتوى بـ `<main>`، و `Privacy.tsx` و `Terms.tsx` يُرجعان `<main>` أيضاً → عنصران متداخلان. قارئات الشاشة تفقد نقطة المعلم (landmark). غيّرهما إلى `<div>` أو `<article>`.

## 21. لا يوجد رابط "تخطي إلى المحتوى"

مستخدم لوحة المفاتيح مضطر للمرور بكل عناصر القائمة في كل صفحة. عنصر أساسي في WCAG 2.4.1.

## 22. قائمة الموبايل بلا Escape ولا حبس تركيز ولا قفل تمرير

`SiteHeader` في `App.tsx:37` — لا `useEffect` للاستماع لـ Escape، ولا focus trap، ولا `overflow: hidden` على الـ body عند الفتح. القائمة المفتوحة تسمح بالتمرير خلفها والوصول للعناصر المخفية بـ Tab.

## 23. `scroll-behavior: smooth` غير مقيّد بتفضيلات الحركة

`index.css:66` — مُطبَّق عالمياً بلا استثناء داخل `@media (prefers-reduced-motion: reduce)`. مزعج جداً لمن لديهم حساسية حركة (dizziness/vestibular).

## 24. لا يوجد ErrorBoundary مركَّب

`client/src/components/ErrorBoundary.tsx` موجود — لكن **لا أحد يستورده**. أي خطأ عرض واحد في أي مكوّن = **شاشة بيضاء تماماً** للزائر. لُفّ `<AppContent />` به فوراً.

## 25. وميض الثيم (FOUC)

`ThemeContext.tsx:31` يطبّق كلاس `.dark` داخل `useEffect` — أي بعد أول رسم. مستخدم الوضع الداكن يرى **ومضة بيضاء ساطعة** في كل تحميل. الحل: سكربت inline صغير في `<head>` يقرأ `localStorage` ويضيف الكلاس قبل تحميل React. و `<meta name="theme-color">` ثابت على `#f5f1ea` — أضف نسخة `media="(prefers-color-scheme: dark)"`.

## 26. رابط "آراء العملاء" في القائمة يقود إلى العدم

`TestimonialsSection.tsx:14` → `if (!isLoading && !hasTestimonials) return null;` — ولا توجد شهادات معتمدة حالياً. إذن `nav.testimonials → /#testimonials` هو **رابط ميت** في القائمة الرئيسية. إما أخفِ العنصر شرطياً أو اعرض حالة فارغة أنيقة.

## 27. صفحة `/contact` غير موجودة في القائمة إطلاقاً

`navItems` تحتوي: الرئيسية، الأعمال، عن هبة، الخدمات، الحجز، آراء العملاء. **لا "تواصل معنا"** — رغم أن الصفحة موجودة وفي الـ sitemap وفي vercel.json. الوصول الوحيد إليها عبر رابط "العودة للتواصل" داخل صفحة الحجز. غير منطقي.

---

# 🟡 القسم الخامس: المحتوى والمصداقية التجارية — الأهم لمجال المقاولات

## 28. كل النصوص العربية موجّهة للمؤنث حصراً ⚠️

"احج**زي** استشارتك" · "شاه**دي** الأعمال" · "تواص**لي** معنا" · "تابع**ينا**" · "ما الذي تتخيل**ينه**" · "راج**عي** طلبك" · "أرس**لي** لنا" · "سنعود إلي**كِ**" · "اكتش**في** المساحة"

هذا خيار تحريري واعٍ على الأرجح، لكنه **مكلف تجارياً**: في مشاريع المقاولات والتشطيبات والمشاريع التجارية (مطاعم، عيادات، مكاتب) متخذ القرار رجل في الأغلب، والمشاريع السكنية غالباً قرار مشترك. الزائر الرجل يشعر أن الموقع "ليس له".

**التوصية:** صيغة محايدة تحافظ على نفس الدفء:
- "احجزي استشارتك" ← **"احجز استشارتك"** أو **"لحجز استشارة"**
- "شاهدي الأعمال" ← **"استعرض الأعمال"**
- "ما الذي تتخيلينه للمكان؟" ← **"ما الذي تتخيله للمكان؟"** أو **"احكِ لنا عن المكان"**
- "سنعود إليكِ خلال 24 ساعة" ← **"نرد خلال 24 ساعة"**

الصيغة المصدرية (احجز/استعرض/تواصل) تحلّ المشكلة بلا أي خسارة في النبرة.

## 29. لا يوجد عمل حقيقي معروض — كل الصور مولّدة بالذكاء الاصطناعي

`client/src/data/projectImages.ts` يوثّق بأمانة أن **كل** الصور الخمس مؤقتة: الهيرو، المشروعان، صورة هبة (رسم توضيحي!)، وحتى **الشعار**. موقع استوديو تصميم داخلي بلا صور أعمال حقيقية = لا مصداقية. هذه أولوية رقم واحد من ناحية المحتوى.

## 30. المشروعان الوحيدان فارغان من البيانات

`client/src/pages/Projects.tsx:26`
```ts
{ slug: "interior", title: "Private Residence", location: "", year: "", intro: "" }
{ slug: "boska",    title: "Boska Café & Restaurant", location: "", year: "", intro: "" }
```
لا موقع، لا سنة، لا مساحة، لا وصف، لا نطاق عمل، لا صور قبل/بعد. صفحة الأرشيف تعرض بطاقتين شبه فارغتين. لوحة الإدارة تدعم كل هذه الحقول — تحتاج فقط ملء البيانات.

## 31. لا شهادات ولا تراخيص ولا إثبات كفاءة

لمجال المقاولات هذا **قاتل**. لا يوجد: عضوية نقابة المهندسين، سجل تجاري، بطاقة ضريبية، سنوات الخبرة، عدد المشاريع المنجزة، شركاء التنفيذ، ضمانات. الزبون المصري الذي سيسلّم شقة بنصف مليون جنيه يبحث عن هذه بالتحديد.

**أضف شريط أرقام في الرئيسية:** `+8 سنوات خبرة` · `42 مشروع منفّذ` · `3 محافظات` · `عضو نقابة المهندسين`

## 32. لا تسعير ولا باقات

سؤال الـ FAQ يقول *"يتم تأكيد سعر الاستشارة حسب نوعها"* — أي لا شيء. حتى نطاق تقريبي ("الاستشارة من 1,500 ج.م" / "التصميم الكامل يبدأ من 250 ج.م/م²") يرفع جودة الليدز بشكل هائل ويوفّر وقتك في محادثات غير جادة.

## 33. لا صفحات خدمات ولا صفحات محلية منفصلة

عندك 3 خدمات في قسم واحد داخل الرئيسية. لـ SEO المحلي تحتاج صفحات مستقلة:
- `/services/interior-design` · `/services/3d-visualization` · `/services/supervision`
- `/qena` · `/luxor` · `/cairo` — صفحات هبوط لكل محافظة تخدمها

هذه هي الطريقة الوحيدة للترتيب على "مهندس ديكور قنا" أو "تشطيب شقق الأقصر".

## 34. صفحة "الموقع" بلا خريطة

`Contact.tsx:205` قسم بعنوان **"STUDIO LOCATION"** يعرض نصاً فقط: "قنا، مصر". ومكوّن `Map.tsx` (155 سطر جاهز) **غير مستخدم في أي مكان**. أضف خريطة مضمّنة + رابط Google Business Profile (وأنشئ الملف إن لم يكن موجوداً — أهم أداة SEO محلي مجانية).

## 35. الفوتر ناقص عناصر الثقة

لا عنوان فعلي، لا ساعات عمل، لا رقم هاتف مباشر (واتساب فقط)، لا خريطة، لا سجل تجاري. لموقع مقاولات هذه معلومات إلزامية.

---

# 🟡 القسم السادس: الأمان والتشغيل

## 36. لا يوجد Rate Limiting على نموذج الحجز

`consultations.create` هو `publicProcedure` محميّ بـ honeypot فقط. أي سكربت بسيط يملأ قاعدة بياناتك بآلاف الطلبات الوهمية. أضف تحديد معدّل بالـ IP (مثلاً 5 طلبات/ساعة) — Vercel KV أو Upstash Redis.

## 37. لا يوجد Content-Security-Policy

`vercel.json` يضبط `X-Content-Type-Options` و `X-Frame-Options` و `Referrer-Policy` و `Permissions-Policy` — لكن CSP غائب تماماً، وهو أهمها ضد XSS. أضف سياسة تسمح بـ `fonts.googleapis.com` و `fonts.gstatic.com` و `'self'`. أضف أيضاً `Strict-Transport-Security`.

## 38. لا يوجد catch-all في إعادة التوجيه — صفحة 404 مكسورة

`vercel.json` يسرد المسارات يدوياً واحداً واحداً. أي مسار غير مذكور (مثل `/blog` أو رابط قديم مكسور) يعطي **صفحة 404 السوداء الافتراضية من Vercel** بالإنجليزية، وليس صفحة `NotFound.tsx` العربية الأنيقة. أضف:
```json
{ "source": "/((?!api/|assets/|images/).*)", "destination": "/index.html" }
```

## 39. لا بديل بريدي لإشعارات الحجز

الاعتماد الكامل على خدمة إشعارات منصة Manus غير المتاحة على Vercel. أضف Resend أو SendGrid كقناة أساسية — **هبة يجب أن يصلها بريد فوراً عند كل طلب**، وإلا فالنظام يعتمد على دخولها اليدوي للوحة الإدارة.

## 40. تجمّع اتصالات MySQL على بيئة Serverless

`server/db.ts:19` ينشئ pool عبر `drizzle(process.env.DATABASE_URL)`. على Vercel كل استدعاء بارد ينشئ اتصالات جديدة → استنفاد حد الاتصالات تحت الحمل. استخدم PlanetScale/TiDB serverless driver أو Prisma Accelerate أو ضع `connectionLimit: 1`.

## 41. لا توجد CI

36 اختباراً ناجحاً و`tsc --noEmit` نظيف — لكن **لا يوجد `.github/workflows/`**. لا شيء يمنع دمج كود مكسور إلى `main`. أضف workflow بسيط: `pnpm check && pnpm test && pnpm build`.

## 42. لا يوجد ESLint

`prettier` موجود لكن لا linting. أضف `eslint` + `eslint-plugin-jsx-a11y` (سيلتقط تلقائياً عدة نقاط من القسم الرابع).

## 43. `license: MIT` وغياب `private: true`

`package.json` لمشروع تجاري خاص برخصة مفتوحة. غيّر إلى `"license": "UNLICENSED"` و `"private": true`.

---

# 🟡 القسم السابع: نظافة المستودع

## 44. كود ميت

| الملف | السطور | الحالة |
|---|---|---|
| `pages/ComponentShowcase.tsx` | 1,437 | غير موجّه، غير مستورد |
| `components/AIChatBox.tsx` | 335 | يستخدمه ComponentShowcase فقط |
| `components/Map.tsx` | 155 | غير مستخدم (وهو مطلوب في صفحة الاتصال!) |
| `components/ManusDialog.tsx` | 89 | غير مستخدم |
| `components/ErrorBoundary.tsx` | 62 | غير مستخدم (**ويجب أن يُستخدم**) |
| `public/__manus__/debug-collector.js` | 25 KB | يُنشر للإنتاج |
| `shared/responsiveImages.ts` | ~110 سطر | إدخالات `/manus-storage/*` ميتة |

## 45. 17 ملف Markdown في الجذر

`todo.md` (40 KB!) · `attachment-13-audit.md` · `glloria-v5-audit.md` · `v7-verification.md` · `v9-audit.md` · `redesign-audit.md` · `brief-gap-audit.md` · `qa-visual-notes.md` · `ideas.md` · `plan-dark-analytics-whatsapp.md` · `glloria-baseline.md` · `glloria-calendar-research.md` · `template.json` · `.gitkeep`

مسودات عمل داخلية معروضة في مستودع يُفترض أنه احترافي. انقلها إلى `docs/archive/` أو احذفها.

---

# 📋 خطة العمل المقترحة

## المرحلة صفر — طوارئ (يوم واحد) 🔴

| # | المهمة | الأثر |
|---|---|---|
| 1 | إصلاح `notifyOwner` كي لا يُسقط الحجز | **الحجوزات تعمل** |
| 2 | ربط نموذج الاتصال أو تحويله لواتساب | إيقاف نزيف الليدز |
| 3 | إصلاح/إزالة سكربت Umami | إيقاف 404 + استعادة البيانات |
| 4 | تركيب `ErrorBoundary` | لا شاشات بيضاء |
| 5 | إضافة catch-all rewrite | صفحة 404 صحيحة |
| 6 | حذف `debug-collector.js` | أمان |

## المرحلة الأولى — الأساسيات (أسبوع) 🟠

7. تحويل القائمة إلى `<Link>` + معالجة روابط `#` · 8. إضافة `/contact` للقائمة · 9. إصلاح `<main>` المتداخل · 10. رابط "تخطي للمحتوى" + Escape/focus-trap للقائمة · 11. تصحيح تباين الألوان · 12. سكربت الثيم inline (إنهاء الوميض) · 13. رؤوس Cache-Control + CSP + HSTS · 14. إصلاح preload بـ `imagesrcset` · 15. `display=swap` + تقليص الخطوط · 16. Rate limiting · 17. إشعارات بريد (Resend)

## المرحلة الثانية — SEO ومحتوى (2–3 أسابيع) 🟠

18. نطاق خاص + `VITE_SITE_URL` · 19. Prerender/SSG + meta لكل صفحة + canonical صحيح · 20. صورة OG مطلقة 1200×630 · 21. FAQPage + Service + Person schema · 22. hreflang + مسارات `/ar` `/en` · 23. sitemap ديناميكي · 24. **تصوير احترافي للمشاريع الحقيقية** · 25. ملء بيانات المشاريع كاملة · 26. جمع 3–5 شهادات عملاء موثّقة · 27. **تحييد صيغة المخاطبة** · 28. Google Business Profile + خريطة في صفحة الاتصال

## المرحلة الثالثة — نمو (شهر+) 🟡

29. صفحات خدمات مستقلة · 30. صفحات محلية (قنا/الأقصر/القاهرة) · 31. باقات وأسعار إرشادية · 32. شريط أرقام الثقة + التراخيص · 33. مدونة/دفتر ملاحظات تصميمي · 34. CI + ESLint · 35. تنظيف الكود الميت والمستودع · 36. فصل CSS الأدمن + تقسيم حزمة Recharts

---

## ما تم التحقق منه في هذه المراجعة

```
✅ pnpm install --frozen-lockfile   → نجح
✅ tsc --noEmit                     → 0 أخطاء
✅ vitest run                       → 36/36 اختبار ناجح
⚠️  vite build                      → نجح مع 3 تحذيرات (Umami + حجم الحزمة)
✅ تشغيل dist/public محلياً          → الصفحات والصور تُخدَم بنجاح
✅ إثبات عملي لخطأ notifyOwner       → THREW: INTERNAL_SERVER_ERROR
✅ حساب نسب تباين WCAG لـ 9 أزواج ألوان
✅ مراجعة يدوية لـ 45+ ملف مصدري
```

---

# ✅ ما تم إصلاحه فعلياً — المرحلة صفر

| # | البند | الملفات | التحقق |
|---|---|---|---|
| 1 | `notifyOwner` لم يعد يُسقط الحجز | `server/_core/notification.ts`، `server/routers.ts` | `notification-resilience.test.ts` (4 اختبارات) |
| 2 | نموذج الاتصال يوصّل الرسالة عبر واتساب | `client/src/pages/Contact.tsx`، `shared/whatsapp.ts` | `Contact.whatsapp.test.ts` (4 اختبارات) |
| 3 | سكربت Umami المكسور | `client/index.html`، `client/src/lib/analytics.ts` | البناء بلا تحذيرات، `dist/index.html` نظيف |
| 24 | تركيب `ErrorBoundary` | `client/src/main.tsx`، `components/ErrorBoundary.tsx`، `index.css` | — |
| 38 | catch-all rewrite + رؤوس Cache/HSTS | `vercel.json` | 17 حالة توجيه مُختبرة |
| 12(جزئي) | حذف `debug-collector.js` + منع فهرسة `/admin` | `client/public/` | غير موجود في `dist` |

## تفاصيل الإصلاحات

### 1. الحجز — `notifyOwner`
الدالة كانت ترمي `TRPCError` عند غياب إعدادات Forge، خلافاً لتوثيقها الخاص. الآن تُرجع `false` وتسجّل تحذيراً. وأُضيف `.catch()` في `routers.ts` كدفاع ثانٍ: الإشعار قناة جانبية ولا يجوز أن يُبطل عملية محفوظة أصلاً.

```
قبل:  THREW: INTERNAL_SERVER_ERROR - Notification service URL is not configured.
بعد:  RESULT (no throw): false
```

### 2. نموذج الاتصال
`handleSubmit` صار يقرأ `FormData` ويبني رابط `wa.me` معبّأ بكل الحقول ويفتحه. لو حجب المتصفح النافذة، نفس الرابط معروض كزر في شاشة التأكيد — **الاستفسار لا يضيع في أي حالة**. ونص التأكيد الكاذب ("وصلت التفاصيل إلى Glloria") استُبدل بنص صادق يشرح الخطوة المتبقية.

> ملاحظة: هذا حل مرحلي صادق يعمل فوراً. الحل النهائي (تخزين الاستفسارات في قاعدة البيانات + إشعار بريدي عبر Resend) في المرحلة الأولى، البند 17.

### 3. التحليلات
الوسم نُقل من HTML إلى `client/src/lib/analytics.ts` ويُحقن وقت التشغيل فقط عند وجود `VITE_ANALYTICS_ENDPOINT` و `VITE_ANALYTICS_WEBSITE_ID`. لا 404 بعد الآن، والتكامل يعمل تلقائياً بمجرد ضبط المتغيرين في Vercel.

### 4. ErrorBoundary
كان موجوداً وغير مستخدم. رُكّب حول جذر التطبيق، وأُعيدت صياغة شاشته: كانت تعرض stack trace خام بالإنجليزية للزائر، والآن رسالة عربية بهوية الموقع + زر إعادة تحميل، والـ stack يظهر في وضع التطوير فقط.

### 5. vercel.json
- 8 قواعد توجيه يدوية ← قاعدة catch-all واحدة (أي رابط مكسور يصل الآن لصفحة 404 العربية)
- `Cache-Control: immutable` لسنة على `/assets/*`، و30 يوماً على `/images/*`
- `Strict-Transport-Security` مُضاف

## نتائج التحقق بعد الإصلاح

```
✅ tsc --noEmit    → 0 أخطاء
✅ vitest run      → 44/44 اختبار (كان 36، +8 جديدة)
✅ vite build      → بلا تحذيرات Umami (بقي تحذير حجم حزمة الأدمن — المرحلة 3)
✅ 17 حالة توجيه   → كلها صحيحة
✅ فحص روابط الصفحة → 7/7 برمز 200، صفر طلبات 404
```

---

# ✅ ما تم إصلاحه — المرحلة الأولى

| # | البند | الملفات |
|---|---|---|
| 4 | القائمة تستخدم تنقل SPA حقيقي بدل إعادة تحميل الصفحة | `hooks/useSectionNavigation.ts`، `App.tsx` |
| 13 | preload الهيرو بـ `imagesrcset` + `imagesizes` | `client/index.html` |
| 14 | الخطوط `display=optional` ← `swap` | `client/index.html` |
| 15 | رؤوس Cache-Control (نُفّذت في المرحلة صفر) | `vercel.json` |
| 19 | تباين الألوان: placeholder و ring | `index.css` |
| 20 | `<main>` المتداخل في Privacy/Terms | `pages/Privacy.tsx`، `pages/Terms.tsx` |
| 21 | رابط «تخطَّ إلى المحتوى» | `App.tsx`، `index.css` |
| 22 | قائمة الموبايل: Escape + حبس تركيز + قفل تمرير | `App.tsx` |
| 23 | `scroll-behavior: smooth` مقيّد بتفضيلات الحركة | `index.css` |
| 25 | إنهاء وميض الثيم + `theme-color` للوضعين | `client/index.html` |
| 26 | رابط «آراء العملاء» الميت أُزيل من القائمة | `App.tsx` |
| 27 | `/contact` أُضيفت للقائمة | `App.tsx`، `LocaleContext.tsx` |

## تفاصيل

### التنقل (النقطة 4 — الأكبر أثراً)
`hooks/useSectionNavigation.ts` يحوّل كل رابط قائمة إلى السلوك الصحيح:
- `/projects` ← تغيير مسار من جهة العميل
- `/#about` ← تمرير للقسم، مع التوجّه للرئيسية أولاً لو كنا في صفحة أخرى

النقرات المعدّلة (Ctrl/Cmd/الزر الأوسط) تُترك للمتصفح فيفتح تبويباً جديداً كالمعتاد. والتمرير ينقل تركيز لوحة المفاتيح مع الشاشة، وإلا كان الضغط التالي على Tab يرجع للهيدر.
كما أن `isNavItemActive` صحّح حالة «الصفحة النشطة» التي كانت معطّلة لثلاثة روابط، مع حماية من تصادم البادئات (`/contact` لا يُفعّل على `/contacts-archive`).

### تباين الألوان (النقطة 19)
| العنصر | قبل | بعد |
|---|---|---|
| placeholder النماذج | `#a39d93` — 2.39:1 ❌ | `#6f6860` — **4.88:1** ✅ |
| `--ring` حلقة التركيز | `#b65e45` — 3.99:1 | `#9e503a` — **5.09:1** ✅ |

`--clay-light` تُرك كما هو: استخدامه الوحيد كنص على خلفية الفوتر الداكنة، وهناك نسبته 5.6:1 ✅

### ملاحظة على «آراء العملاء»
`TestimonialsSection` يُرجع `null` عند عدم وجود شهادات معتمدة، فكان الرابط ميتاً. أزلته من القائمة ووضعت `/contact` مكانه (كانت مفقودة تماماً). القسم نفسه لم يُحذف — يكفي إعادة الرابط بعد اعتماد أول شهادة.

## نتائج التحقق

```
✅ tsc --noEmit    → 0 أخطاء
✅ vitest run      → 52/52  (36 أصلية + 16 جديدة)
✅ vite build      → بلا تحذيرات عدا حجم حزمة الأدمن
✅ 7/7 مسارات      → 200، شاملة المسار غير الموجود (صفحة 404 الداخلية)
✅ 7/7 روابط أصول  → 200، صفر 404
✅ تباين           → 3/3 نجحت معايير WCAG AA
```

---

# ✅ ما تم إصلاحه — المرحلة الثانية (الجزء التقني)

| # | البند | الملفات |
|---|---|---|
| 5، 6 | عنوان ووصف و canonical لكل صفحة | `lib/seo.ts`، `hooks/useSeo.ts` + 7 صفحات |
| 7 | hreflang لـ ar / en / x-default | `lib/seo.ts` |
| 10 | رقم الهاتف بصيغة E.164 في JSON-LD | `shared/contact.ts`، `index.html` |
| 11 | صفحة 404 كانت **عربية فقط** | `pages/NotFound.tsx` |
| 12 | الكود الميت في عنوان الرئيسية | `pages/Home.tsx` |
| — | **FAQPage schema** لأسئلة الرئيسية الست | `hooks/useFaqSchema.ts` |
| 28 | **صيغة المخاطبة المؤنثة** | 8 ملفات + اختبار يمنع رجوعها |
| 36 | Rate limiting على نموذج الحجز | `_core/rateLimit.ts`، `routers.ts` |
| 37 | Content-Security-Policy | `vercel.json` |

## تفاصيل

### SEO لكل صفحة (5، 6، 7)
كان الـ canonical **مثبتاً على الصفحة الرئيسية في كل المسارات**. جوجل يعامل الـ canonical كتعليمة، أي أن `/projects` و `/booking` و `/contact` وكل صفحات المشاريع كانت تقول له حرفياً «الصفحة الحقيقية هي الرئيسية» — طلب فعلي بحذفها من الفهرس.

الآن لكل مسار: عنوان ووصف خاصان بلغتين، canonical فريد، Open Graph و Twitter، و hreflang. صور OG صارت **مطلقة** (فيسبوك وواتساب يرفضان النسبية). المسارات تُنظَّف من `?utm_source` و `#hash` فلا يتنافس رابطان على نفس الصفحة.

تحقق فعلي:
```
PASS  /                                     -> .../
PASS  /projects                             -> .../projects
PASS  /booking                              -> .../booking
PASS  /contact                              -> .../contact
PASS  /projects/boska?utm_source=fb#gallery -> .../projects/boska
```

> **حد هذا الإصلاح — مهم:** الكود يعمل في المتصفح. جوجل ينفّذ JavaScript وسيلتقط هذه الوسوم، لكن **كاشطات التواصل الاجتماعي (فيسبوك، واتساب، تويتر) لا تنفّذ JS إطلاقاً** — تقرأ HTML الخام فقط. فمعاينة الروابط ما زالت تعتمد على قيم `index.html`. الحل الكامل هو prerender لكل مسار وقت البناء، و`lib/seo.ts` مكتوب ليُعاد استخدامه في تلك الخطوة بلا تغيير.

### صيغة المخاطبة (28)
28 موضعاً كانت تخاطب المؤنث حصراً. لاستوديو أرشيفه يشمل مطاعم وكافيهات وعيادات، متخذ القرار كثيراً ما يكون رجلاً، وفي السكني القرار مشترك عادةً.

| قبل | بعد |
|---|---|
| احجزي استشارتك | **احجز استشارتك** |
| شاهدي الأعمال | **استعرض الأعمال** |
| تواصلي معنا | **تواصل معنا** |
| ما الذي تتخيلينه للمكان؟ | **ما الذي تتخيله للمكان؟** |
| سنعود إليكِ خلال 24 ساعة | **نعود إليك خلال 24 ساعة** |

`server/inclusive-copy.test.ts` يفحص كل ملفات المصدر مقابل 16 صيغة مؤنثة ويُفشل البناء عند رجوع أي منها — وقد أمسك بالفعل موضعاً فاتني في `AdminAnalytics.tsx`.

### Rate limiting (36)
نافذة منزلقة: **5 طلبات/ساعة لكل IP**. الحالة في ذاكرة الوحدة — على Vercel كل نسخة لها عدّادها وتُصفَّر عند البدء البارد، فهذا يرفع تكلفة العبث ولا يضمن سقفاً عالمياً. مقصود: لا يحتاج بنية تحتية جديدة ولا يمكنه إسقاط الموقع. عند ظهور عبث حقيقي يُستبدل المخزن بـ Vercel KV دون تغيير موضع الاستدعاء.

### CSP (37)
12 فحصاً آلياً على السياسة: `default-src 'self'`، `frame-ancestors 'none'` (نقر مخادع)، `object-src 'none'`، `base-uri 'self'` (حقن وسم base)، `form-action 'self'` (خطف النماذج)، مع سماح محدّد لخطوط جوجل فقط.

## نتائج التحقق

```
✅ tsc --noEmit    → 0 أخطاء
✅ vitest run      → 107/107  (36 أصلية + 71 جديدة)
✅ vite build      → نظيف
✅ CSP             → 12/12 فحص
✅ canonical       → 5/5 مسارات فريدة، صور OG مطلقة
```

---

# ✅ ما تم إصلاحه — المرحلة الثالثة (التقنية)

| # | البند | النتيجة |
|---|---|---|
| 17 | حزمة الأدمن 523 KB | **130 KB** (‑75%) |
| — | CSS الأدمن على الصفحات العامة | **‑16 KB** من كل زيارة |
| 11، 16 | sitemap ثابت برابطين | **12 رابطاً** يُولَّد آلياً |
| 41 | لا CI | 5 بوابات على كل دفعة |
| 43 | `license: MIT` لمشروع تجاري | `UNLICENSED` + `private` |
| 44 | كود ميت | **‑1,861 سطراً** |
| 9 | النطاق مثبّت في robots/sitemap | متغيّر `SITE_URL` |

## قياسات البناء

| الأصل | قبل | بعد | الفرق |
|---|---|---|---|
| `Admin-*.js` | 523.0 KB | **130.6 KB** | **‑75%** |
| `AdminAnalytics-*.js` | — | 405.6 KB | يُحمَّل عند فتح تبويب التحليلات فقط |
| `index-*.css` (كل زائر) | 191.7 KB | **175.6 KB** | ‑16.2 KB |
| `Admin-*.css` | — | 16.2 KB | على `/admin` فقط |
| تحذير «حزمة > 500 KB» | ⚠️ موجود | **✅ اختفى** | |

### كيف
- **Recharts** كان مضمّناً في حزمة الأدمن كلها رغم أنه لا يلزم إلا عند فتح تبويب التحليلات → `lazy()` + `Suspense`.
- **`scripts/split-admin-css.py`** ينقل قواعد الأدمن من `index.css` إلى `admin.css` المستورد من `Admin.tsx`، فيصدره Vite كحزمة منفصلة.

قاعدة الأمان في المقسّم: القاعدة تُنقل **فقط** إذا كانت كل محدداتها خاصة بالأدمن. أي قاعدة تخلط محدداً إدارياً بآخر عام تبقى مكانها — ترك قاعدة زائدة غير ضار، بينما نقل قاعدة عامة بالخطأ يكسر الموقع.

تحقق آلي بعد التقسيم:
```
قواعد قبل التقسيم : 1036
قواعد بعد التقسيم : 1036
مفقودة: 0   زائدة: 0   ✅ لا فقد
```
وفُحصت 176 فئة مستخدمة في الصفحات العامة — لم تُنقل أي منها.

### الكود الميت المحذوف
| الملف | الأسطر |
|---|---|
| `pages/ComponentShowcase.tsx` | 1,437 |
| `components/AIChatBox.tsx` | 335 |
| `components/ManusDialog.tsx` | 89 |

### sitemap ديناميكي
كان ملفاً ثابتاً برابطين، بلا `lastmod`، والنطاق مكتوب داخله. الآن `scripts/generate-sitemap.mjs` يقرأ المسارات وأسماء المشاريع من `realProjects.ts` ويعمل ضمن `pnpm build`، فأي مشروع يُضاف يُنشر لمحركات البحث تلقائياً. النطاق من `SITE_URL`.

```
sitemap: 12 urls
  /  /projects  /booking  /contact  /privacy  /terms
  /projects/{boska, elite, sara-alaa, classic-white-clinic,
             private-villa, private-residence}
```

### CI
`.github/workflows/ci.yml` — تنسيق، فحص أنواع، اختبارات، بناء، وفحص CSP. الاختبارات كبرت من 36 إلى 107 ولم يكن هناك ما يفرضها.

## نتائج التحقق

```
✅ prettier --check → كل الملفات مطابقة
✅ tsc --noEmit     → 0 أخطاء
✅ vitest run       → 107/107
✅ vite build       → بلا أي تحذير
✅ CSP              → 12/12
✅ سلامة CSS        → 1036 = 1036 قاعدة، صفر فقد
```
