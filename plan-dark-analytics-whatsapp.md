# خطة تطوير Glloria: التحليلات والوضع الداكن وWhatsApp

## الهدف

تطوير النسخة الحالية من موقع Glloria بإضافة لوحة تحليلات تفاعلية للحجوزات والمشاريع، وتفعيل الوضع الداكن على الموقع العام ولوحة الإدارة، وتحسين تجربة ما بعد إرسال نموذج الحجز بفتح WhatsApp برسالة مُعبأة مسبقاً إلى رقم WhatsApp Business الخاص بـ Glloria: **+20 10 66646397**.

سيتم الحفاظ على هوية **Warm Editorial**، ودعم العربية RTL والإنجليزية LTR، وعدم نشر أي بيانات تجريبية أو كشف أسرار الخادم في الواجهة.

## القرار الخاص بWhatsApp

بناءً على موافقة المستخدم، سيتم تنفيذ **تسليم يدوي سريع** بدلاً من إرسال آلي من الخادم. بعد نجاح حفظ الحجز، ستظهر حالة نجاح واضحة وبداخلها زر يفتح محادثة WhatsApp مع رقم Glloria Business مع نص مُعبأ مسبقاً يتضمن رقم الطلب وبياناته الأساسية. سيظل الضغط على زر الإرسال النهائي بيد المستخدم أو الإدارة؛ لذلك لن ندّعي أن الرسالة أُرسلت تلقائياً أو أن التسليم مضمون.

| الخيار | تجربة المستخدم | المتطلبات | المزايا | الحدود |
|---|---|---|---|---|
| **الخيار المعتمد: WhatsApp برسالة مُعبأة مسبقاً** | بعد نجاح الحجز يضغط المستخدم على زر التواصل، ثم يراجع الرسالة ويرسلها | لا يحتاج API أو بيانات اعتماد خارجية | سريع، منخفض التعقيد، لا يضع أسراراً في المشروع، ويستفيد من WhatsApp Business الحالي | ليس إرسالاً آلياً من الخادم، ولا يوفّر حالة تسليم مؤكدة |
| Meta WhatsApp Cloud API أو مزود مثل Twilio | الخادم يرسل رسالة فعلية بعد الحفظ | رقم مُرسل موثق، بيانات API، موافقة/opt-in، وقالب رسالة معتمد عند الحاجة | إرسال خادمي حقيقي، ويمكن لاحقاً تسجيل حالة الإرسال والتسليم | إعداد أعقد، اعتماد على مزود خارجي وتكلفة/إدارة اعتماديات، ويحتاج اختباراً ومراقبة إضافية |

سيبقى خيار API مؤجلاً كترقية مستقبلية فقط. توثيق Meta يوضح أن Cloud API مخصص للإرسال البرمجي واستقبال webhooks، وأن الرسائل خارج نافذة خدمة العميل قد تحتاج قوالب معتمدة؛ لذلك لن يتم افتراض هذا التكامل أو محاكاته دون إعداد رسمي من المستخدم. [توثيق بدء WhatsApp Cloud API](https://developers.facebook.com/documentation/business-messaging/whatsapp/get-started) [توثيق إرسال الرسائل](https://developers.facebook.com/documentation/business-messaging/whatsapp/messages/send-messages)

## نطاق التنفيذ

### 1. تحليلات لوحة الإدارة

سيتم إضافة طبقة تحليلات فعلية فوق بيانات Drizzle/MySQL الحالية، مع الاستفادة من حقلي `createdAt` الموجودين في جدولي `consultationRequests` و`projects`. ستُضاف دوال تجميع في `server/db.ts` وإجراءات محمية في `server/routers.ts` حتى لا تكون الأرقام محسوبة في المتصفح من قائمة ناقصة أو من بيانات غير مصرح بها.

سيُرجع الإجراء بيانات شهرية مع ملء الأشهر التي لا تحتوي على سجلات بأصفار، باستخدام UTC داخلياً لتجنب اختلافات المنطقة الزمنية. النطاق الافتراضي سيكون آخر 12 شهراً، مع خيارات 6 و12 و24 شهراً، ويُراعى أن تظهر حالة فارغة مفهومة إذا لم توجد بيانات.

ستتضمن صفحة التحليلات على الأقل:

| الرسم | البيانات | التفاعل |
|---|---|---|
| الحجوزات بمرور الوقت | عدد الطلبات الجديدة شهرياً، مع إمكانية فصلها حسب الحالة | تمرير المؤشر يعرض الشهر والقيم، ومفتاح لإظهار/إخفاء السلاسل |
| نمو المشاريع بمرور الوقت | عدد المشاريع المنشأة شهرياً مع تقسيم Interior / Architectural | تمرير المؤشر، مفتاح السلاسل، وفلاتر النطاق الزمني ونوع التصميم |
| ملخص حالة الحجوزات | توزيع الحالات الحالية: جديد، مراجعة، تواصل، مؤكد، مكتمل، ملغي | تمرير المؤشر وعرض العدد والنسبة، مع الحفاظ على ألوان الحالة الحالية |

سيتم وضع التحليلات في أعلى لوحة الإدارة أو في تبويب مستقل باسم **التحليلات** مع بقاء تبويبات المشاريع والتقييمات والحجوزات كما هي. سيتم استخدام مكوّن `client/src/components/ui/chart.tsx` المبني على Recharts، مع tooltips وlegend متوافقين مع العربية والوضعين الفاتح والداكن، وتجهيز تخطيط متجاوب لا يسبب تمريراً أفقياً على الهاتف.

الملفات المتوقعة للتعديل هي `server/db.ts`، و`server/routers.ts`، و`client/src/pages/Admin.tsx`، و`client/src/components/AdminBookings.tsx`، وقد يُستخرج مكوّن مستقل مثل `AdminAnalytics.tsx` لتقليل حجم الملفات وتحسين قابلية الصيانة. لن تتم إضافة جدول جديد ما دام التجميع يمكن تنفيذه من timestamps الموجودة؛ وإذا أظهر اختبار الأداء أن التجميع المباشر غير كافٍ، ستُدرس فهارس أو جدول ملخص لاحقاً بدلاً من إدخال تعقيد غير ضروري الآن.

### 2. الوضع الداكن في جميع الصفحات

يوجد بالفعل `ThemeProvider` في `client/src/contexts/ThemeContext.tsx`، لكنه غير موصول حالياً بغلاف التطبيق. سيتم تفعيله على مستوى التطبيق مع `switchable`, وتثبيت الاختيار في `localStorage`، وإضافة زر واضح ومتاح بلوحة المفاتيح في رأس الموقع العام. ستتم إضافة زر أو عنصر مكافئ داخل `DashboardLayout` حتى لا يحتاج المستخدم إلى العودة للموقع لتغيير الثيم أثناء إدارة المحتوى.

سيتم توسيع متغيرات CSS في `client/src/index.css` بإضافة لوحة داكنة دافئة متوافقة مع ألوان الطين والزيتون الحالية، ثم مراجعة الخلفيات والنصوص والحدود والأزرار والروابط والنماذج والجداول والبطاقات ورسوم Recharts. لن يتم الاعتماد على ألوان ثابتة داخل المكونات عندما يكون متغير دلالي مناسباً متاحاً.

سيتم الحفاظ على الوضع الفاتح كافتراضي للمستخدم الجديد، مع احترام الاختيار المحفوظ لاحقاً. وستُراجع قابلية القراءة والتباين، ومؤشرات التركيز، وحالة `prefers-reduced-motion`، وسلوك القائمة المنسدلة والتنقل على الهاتف في كلا الوضعين. إذا لزم منع وميض الثيم عند التحميل، ستتم إضافة تهيئة صغيرة آمنة قبل العرض بدلاً من تغيير بنية التطبيق بالكامل.

الملفات المتوقعة للتعديل هي `client/src/App.tsx`، و`client/src/main.tsx` أو موضع providers الحالي، و`client/src/components/DashboardLayout.tsx`، و`client/src/contexts/ThemeContext.tsx` عند الحاجة، و`client/src/index.css`، إضافة إلى أي مكوّن مشترك لرأس الموقع أو الإدارة.

### 3. تجربة WhatsApp بعد نجاح الحجز

سيتم تحديث مسار نجاح `Booking.tsx` بحيث يبقى الحجز محفوظاً أولاً في قاعدة البيانات، ثم تظهر رسالة نجاح مستقلة لا تعتمد على نجاح فتح WhatsApp. ستتضمن الرسالة رقم الطلب إن أعاده الخادم، وتوضح أن الطلب قيد المراجعة، وتعرض زر **متابعة عبر WhatsApp** يفتح رابطاً من الشكل `wa.me/201066646397?text=...` مع `encodeURIComponent`.

سيحتوي النص المُعبأ مسبقاً على أقل قدر لازم من التفاصيل: اسم العميل، رقم الطلب، المدينة أو الخدمة، والتاريخ والوقت المقترحان. لن يوضع وصف المشروع الكامل أو معلومات حساسة في رابط خارجي إلا إذا كان ذلك ضرورياً، لأن روابط WhatsApp قد تظهر في سجل المتصفح أو أدوات التحليل. سيُضاف أيضاً نص بديل يوضح أن المستخدم يحتاج إلى مراجعة الرسالة والضغط على إرسال، وسيظل إشعار المالك الحالي يعمل كمسار داخلي مستقل.

سيُستخدم الرقم الجديد في رابط تأكيد WhatsApp فقط ضمن هذا النطاق، بينما سيُحافظ على رقم التواصل العام الحالي في باقي أزرار الموقع ما لم يطلب المستخدم صراحة استبداله في جميع نقاط الاتصال. هذا القرار يمنع تغيير قناة الاتصال العامة بالخطأ أثناء تنفيذ ميزة التأكيد.

## خطوات التنفيذ

أولاً، تُضاف نماذج الإدخال والتحقق الخاصة بنطاق التحليلات إلى الإجراء المحمي، مع حماية الصلاحيات من خلال `adminProcedure` وإرجاع بيانات مجمعة فقط. ثانياً، تُبنى واجهة التحليلات فوق مكوّنات الرسوم الموجودة، مع loading وempty وerror states وتصفية نطاق زمني مستقرة لا تتسبب في إعادة طلب لا نهائية.

بعد ذلك، يُوصل `ThemeProvider` بالغلاف العام، ويُضاف مفتاح الثيم في الرأس العام ولوحة الإدارة، ثم تُراجع متغيرات CSS والمكوّنات ذات الألوان المخصصة. أخيراً، تُحدّث حالة نجاح الحجز ورابط WhatsApp المعبأ مسبقاً، ويُضاف اختبار مستقل يتأكد من ترميز الرقم والنص دون تنفيذ إرسال خارجي.

لن يتم تعديل قاعدة البيانات إلا إذا أثبت التصميم حاجة فعلية إلى حقول جديدة. وفي حال احتاجت التحليلات إلى migration، سيتم اتباع دورة المشروع المعتادة: تحديث schema، توليد migration، تطبيقها على قاعدة البيانات، ثم اختبار الإجراء قبل النشر.

## خطة الاختبار

سيتم تشغيل فحص TypeScript، واختبارات Vitest الحالية والمضافة، وproduction build. وستُضاف اختبارات للخادم تتحقق من أن المستخدم غير الإداري لا يستطيع قراءة التحليلات، وأن التجميع يملأ الفترات الفارغة، وأن نتائج المشاريع والحجوزات لا تخلط الحالات أو أنواع التصميم.

على مستوى المتصفح، سيتم التحقق من أن الرسوم تعرض tooltips صحيحة، وأن تغيير النطاق الزمني يعيد البيانات المطلوبة فقط، وأن الفلاتر لا تكسر الجدول أو بطاقات الإحصاءات. كما سيُختبر الوضع الداكن على الصفحة الرئيسية، المشاريع، تفاصيل المشروع، الحجز، الخصوصية، الشروط، `/admin`، وتبويبات لوحة الإدارة، مع فحص النصوص العربية والإنجليزية وعدم وجود تمرير أفقي.

سيتم اختبار نموذج الحجز بمسار نجاح حقيقي غير حساس، ثم التأكد من أن رسالة النجاح تظهر حتى لو تعذر فتح WhatsApp، وأن الرابط يوجه إلى الرقم `201066646397` ويحتوي نصاً مُرمزاً صحيحاً. لن يتم إرسال رسالة فعلية إلى عميل أو رقم خارجي أثناء الاختبار؛ سيتم فحص الرابط وسلوك الواجهة فقط.

كما ستتم مراجعة إمكانية الوصول، بما في ذلك أسماء أزرار الثيم والرسوم، التنقل بلوحة المفاتيح، التباين في الوضع الداكن، حالات التركيز، واحترام تقليل الحركة. وبعد اكتمال الاختبارات سيتم أخذ لقطات سطح المكتب والهاتف وحفظ checkpoint جديد قبل التسليم.

## معايير القبول

يُعتبر التحديث جاهزاً عندما تعرض لوحة الإدارة رسوماً فعلية ومتجاوبة للحجوزات والمشاريع بمرور الوقت مع فلاتر ونوافذ معلومات، ويعمل تبديل الوضع الداكن عبر جميع الصفحات ويحفظ الاختيار، وتظهر بعد نجاح الحجز رسالة واضحة مع رابط WhatsApp مُعبأ مسبقاً إلى الرقم الجديد دون ادعاء إرسال آلي، وتنجح اختبارات TypeScript وVitest وproduction build، ولا تتأثر حماية لوحة الإدارة أو خصوصية بيانات الحجوزات.

## الافتراضات والمخاطر المفتوحة

الافتراض الأساسي أن المستخدم يريد تجربة WhatsApp اليدوية التي وافق عليها، وليس تكامل Meta API فعلياً في هذه المرحلة. لذلك لا يحتاج التنفيذ الحالي إلى token أو phone-number-id أو قالب Meta. إذا أصبح الإرسال الخادمي مطلوباً لاحقاً، فسيكون مشروعاً منفصلاً يتطلب بيانات اعتماد رسمية، موافقة على القوالب، سياسة opt-in، وتسجيل حالات الإرسال والفشل وإعادة المحاولة.

كما يُفترض أن رقم `+20 10 66646397` هو رقم Glloria Business المخصص لمسار التأكيد. سيُستخدم بصيغة دولية بدون علامة `+` في رابط WhatsApp. ويظل ربط Google Calendar خارج هذا التحديث، كما كان مؤجلاً سابقاً إلى حين تأكيد حساب Heba وطريقة الحجز المطلوبة.

## المراجع

1. [Meta for Developers — WhatsApp Cloud API Get Started](https://developers.facebook.com/documentation/business-messaging/whatsapp/get-started)
2. [Meta for Developers — WhatsApp Send Messages](https://developers.facebook.com/documentation/business-messaging/whatsapp/messages/send-messages)
